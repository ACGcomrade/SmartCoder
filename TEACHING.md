# SmartCoder 团队入门手册

> 目标读者：学过微积分和基础线性代数概念的理工科同学（大一或大二水平）。  
> 读完本文你应能：手算项目 base case、看懂核心代码、独立运行现有实验、提出并实现一个新方向。

---

## 第 0 章：先确认你需要的基础知识

你需要知道（我们会用到，但下面会重新解释）：

- **整数除法和余数**：`17 ÷ 5 = 3 余 2`，这就是 `17 mod 5 = 2`
- **矩阵/向量**：知道"元素""行列"的概念即可，本项目不做矩阵乘法
- **Python 基础**：能读懂 `for` 循环和列表

不需要知道：群论、抽象代数、信息论（遇到相关术语时文中会简单解释）。

---

## 第 1 章：这个项目在做什么

**一句话**：研究能否用一个"共享的大整数矩阵"同时存储多张照片，并用不同的数学"解码器"分别取出每张照片。

**背景直觉**：

想象一个特殊的调色盘，你把三种颜色（红、绿、蓝）叠加在里面，看上去是棕色。如果你有一个"红色滤镜"，能把红色分离出来；一个"绿色滤镜"，能把绿色分离出来。SmartCoder 做的事情类似：把多张照片的像素信息"叠加"存入同一个数字矩阵（叫做 **base**），用不同的数学函数（叫做**解码器 decoder**，对应不同的**域 field**）把每张照片分离出来。

**项目的核心问题**（不预设答案，这是研究课题）：

1. 这种结构能精确存储和恢复照片吗？（v0.3 答：**能**，误差 ≤ 2 个灰阶）
2. 能减少存储空间吗？（v0.5 答：**最多约 2.8 倍压缩**）
3. 从 base 里能读出"哪些照片内容相关"的信号吗？（Direction 1 答：**能**，可测量的相关性差距）
4. "把多张照片放进同一个域"的耦合结构有什么边界？（Direction 2 答：**精确可计算**）

---

## 第 2 章：数学基础（从零开始）

### 2.1 模运算（mod）

**定义**：`a mod m`（读作"a 模 m"）= 用 m 除 a 后的**余数**。

```
17 mod 5 = 2      (因为 17 = 3×5 + 2)
20 mod 4 = 0      (因为 20 = 5×4 + 0)
3  mod 7 = 3      (因为 3 = 0×7 + 3)
```

**有用的性质**（不需要记忆公式，理解意思即可）：

```
(a + b) mod m = ((a mod m) + (b mod m)) mod m
(a × b) mod m = ((a mod m) × (b mod m)) mod m
```

举例：`(11 × 9) mod 17`  
方法一：`99 mod 17 = 99 - 5×17 = 99 - 85 = 14`  
方法二：`(11 mod 17) × (9 mod 17) mod 17 = 11×9 mod 17 = 99 mod 17 = 14` ✓

**模逆元**：如果 `a×b ≡ 1 (mod m)`，则称 b 是 a 模 m 的逆元，写作 $a^{-1} \bmod m$。

例子：`2×9 = 18 ≡ 1 (mod 17)`，所以 `2⁻¹ mod 17 = 9`。

验证：`18 mod 17 = 1` ✓

*注：不是所有数都有模逆元。当 `gcd(a, m) = 1`（a 和 m 互质）时，a 对 m 的模逆元存在。素数 m 时，1 到 m-1 的所有整数都有模逆元。*

---

### 2.2 素数域 $\mathbb{F}_p$

**素数（prime number）**：只能被 1 和自身整除的正整数（2, 3, 5, 7, 11, 13, 17, 19, ...）。

**素数域 $\mathbb{F}_p$**（也写作 $\mathbb{Z}/p\mathbb{Z}$，读"Z 模 p Z"）：取素数 p，把集合 $\{0, 1, 2, \ldots, p-1\}$ 上的加法和乘法都做模 p 运算，就得到一个"有限域"。

直觉：一个"绕圈的钟表"，数到 p 就归零。

```
在 F₁₇ 中：
  11 + 9 = 20 mod 17 = 3
  11 × 2 = 22 mod 17 = 5
  11⁻¹   = ?  (找 x 使得 11x ≡ 1 mod 17)
           11×14 = 154 = 9×17 + 1, 所以 11⁻¹ mod 17 = 14
```

*本项目用到的素数都 ≥ 4096，但运算原理和上面的小例子完全一样。*

---

### 2.3 中国剩余定理（CRT）

**这是项目的核心数学工具。**

**定理**（非形式版本）：选两个互质的整数 $p_0, p_1$（比如两个不同的素数），则对任意 $r_0 \in [0, p_0)$ 和 $r_1 \in [0, p_1)$，在 $[0, p_0 \times p_1)$ 里恰好存在唯一一个整数 $b$ 满足：

$$b \bmod p_0 = r_0 \quad \text{且} \quad b \bmod p_1 = r_1$$

用"钟表"类比：假设你有一个 17 小时制的钟和一个 19 小时制的钟同时走。如果你告诉我"17 小时钟现在指着 11，19 小时钟现在指着 9"，我能唯一确定现在是 0 到 322（=17×19-1）之间的某个时刻。

**CRT 重建公式**：

设 $M = p_0 \times p_1$，定义 CRT 权重（幂等元）：

$$e_0 = \frac{M}{p_0} \times \left(\frac{M}{p_0}\right)^{-1} \!\!\!\! \bmod p_0 \times \frac{1}{1} \quad \text{（在模 } M \text{ 下计算）}$$

简便写法：

$$e_k = \left(\frac{M}{p_k}\right) \times \left[\left(\frac{M}{p_k}\right)^{-1} \bmod p_k\right]$$

则：$b = (r_0 \times e_0 + r_1 \times e_1) \bmod M$

**验证权重的性质**（必须满足）：

- $e_0 \bmod p_0 = 1$，$e_0 \bmod p_1 = 0$
- $e_1 \bmod p_0 = 0$，$e_1 \bmod p_1 = 1$

---

## 第 3 章：SmartCoder 的存储构造

### 3.1 量化：把像素变成小整数

一张普通照片的每个像素（pixel）有三个通道：R（红）、G（绿）、B（蓝），每个通道是 0 到 255 的整数（8 位）。

SmartCoder 把它压缩到 **6 位**：

$$\text{量化值} = \lfloor \text{原始像素} / 4 \rfloor \in [0, 64)$$

恢复时：$\text{近似原始值} = \text{量化值} \times 4 + 2$

**误差**：最大 ±2 个灰阶，在 PSNR（信噪比）上约为 46 dB，肉眼无法分辨。

**统一记号**：量化精度 $Q = 64$，量化后的像素值 $d \in \{0, 1, \ldots, 63\}$。

---

### 3.2 打包：把两张照片放进一个域

每个"域"（field）负责存储 **2 张照片**（槽 slot 0 和槽 slot 1）。

在 base 的每个 cell（位置 $(i, j, \text{通道})$），把这 2 张照片的像素打包成一个"残数"：

$$r_k = d_0 + Q \times d_1 = d_0 + 64 \times d_1$$

其中 $d_0, d_1 \in [0, 64)$，所以 $r_k \in [0, 64^2) = [0, 4096)$。

选取素数 $p_k \geq Q^2 = 4096$，则 $r_k$ 能被 $p_k$ 安全地"容纳"。

**解包**（从 $r_k$ 还原 $d_0, d_1$）：

$$d_0 = r_k \bmod 64, \qquad d_1 = \lfloor r_k / 64 \rfloor$$

---

### 3.3 CRT 编码：用一个大整数存所有域的信息

有 $K$ 个域，就选 $K$ 个互质素数 $p_0, p_1, \ldots, p_{K-1}$（每个 $\geq 4096$）。

**总模数**：$M_K = p_0 \times p_1 \times \cdots \times p_{K-1}$

**base 中每个 cell 的值**：通过 CRT 将所有域的残数合并成一个大整数 $b$：

$$b = \left(\sum_{k=0}^{K-1} r_k \times e_k\right) \bmod M_K$$

其中 $e_k$ 是第 $k$ 个域的 CRT 权重（计算方法同 §2.3）。

**解码第 $k$ 个域**（不需要知道其他域的信息）：

$$r_k = b \bmod p_k \quad \Rightarrow \quad d_0 = r_k \bmod 64, \quad d_1 = \lfloor r_k / 64 \rfloor$$

**为什么这样能工作？** CRT 的双向一一对应保证：知道所有 $r_k$ 能唯一恢复 $b$；反过来，$b \bmod p_k = r_k$，每个域的信息都精确嵌在 $b$ 里，互不干扰。

---

### 3.4 完整手算例子

**参数设置**（足够小，可以用纸笔算）：

```
Q = 4（2 位量化，像素 0-3，比真实的 64 小但原理相同）
K = 2（两个域，共 4 张照片）
p₀ = 17  (最小的 ≥ Q² = 16 的素数)
p₁ = 19  (下一个素数)
M = 17 × 19 = 323
```

**第一步：计算 CRT 权重**

```
M/p₀ = 323/17 = 19
19⁻¹ mod 17 = ?  →  2×9=18≡1(mod 17)  →  19≡2(mod 17)  →  2⁻¹=9
e₀ = 19 × 9 = 171

M/p₁ = 323/19 = 17
17⁻¹ mod 19 = ?  →  17×9=153=8×19+1  →  17⁻¹=9
e₁ = 17 × 9 = 153

验证：
  e₀ mod 17 = 171 mod 17 = 171-10×17=1 ✓    e₀ mod 19 = 171-9×19=0 ✓
  e₁ mod 17 = 153-9×17=0 ✓                   e₁ mod 19 = 153-8×19=1 ✓
```

**第二步：存入 4 张照片的某一个像素位置**

```
照片 0（域 0，槽 0）：像素值 d₀ = 3
照片 1（域 0，槽 1）：像素值 d₁ = 2
照片 2（域 1，槽 0）：像素值 d₂ = 1
照片 3（域 1，槽 1）：像素值 d₃ = 2

打包：
  r₀ = 3 + 4×2 = 11   （域 0 的残数）
  r₁ = 1 + 4×2 = 9    （域 1 的残数）

CRT 编码（base 中的一个 cell 值）：
  b = (11×171 + 9×153) mod 323
    = (1881 + 1377) mod 323
    = 3258 mod 323

  3258 ÷ 323 = 10.08...  → 商 10
  3258 - 10×323 = 3258 - 3230 = 28

  b = 28
```

**第三步：验证**

```
b mod p₀ = 28 mod 17 = 28 - 17 = 11 ✓ （= r₀）
b mod p₁ = 28 mod 19 = 28 - 19 =  9 ✓ （= r₁）
```

**第四步：解码**

```
读取照片 0（域 0，槽 0）：
  r₀ = b mod 17 = 11
  d₀ = 11 mod 4 = 3 ✓

读取照片 1（域 0，槽 1）：
  r₀ = 11（同上）
  d₁ = 11 ÷ 4 = 2（整除） ✓

读取照片 2（域 1，槽 0）：
  r₁ = b mod 19 = 9
  d₂ = 9 mod 4 = 1 ✓

读取照片 3（域 1，槽 1）：
  r₁ = 9
  d₃ = 9 ÷ 4 = 2 ✓
```

全部正确！4 张照片的这个像素位置，用一个 `b = 28` 精确编码了。

---

### 3.5 局部修改：只改一张照片的一个区域

假设照片 0 想把像素值从 3 改成 1（只改这一个位置，不影响其他任何照片）。

**CRT 幂等元更新公式**：

$$b' = \left(b + (d_{\text{new}} - d_{\text{old}}) \times Q^{\text{slot}} \times e_k\right) \bmod M$$

手算（照片 0 是域 0 槽 0，所以 $k=0$，slot=0）：

```
d_new = 1, d_old = 3, Q^slot = 4^0 = 1
Δ = (1 - 3) × 1 × 171 mod 323
  = (-2) × 171 mod 323
  = -342 mod 323
  = -342 + 323 = -19
  = -19 + 323 = 304  （加 323 直到非负）
  不对，-342 mod 323：-342 + 2×323 = -342 + 646 = 304

b' = (28 + 304) mod 323 = 332 mod 323 = 9
```

**验证**：

```
b' mod 17 = 9 mod 17 = 9
解包：d₀ = 9 mod 4 = 1 ✓ (新值)
     d₁ = 9 ÷ 4 = 2 ✓ (照片 1 未变)

b' mod 19 = 9 mod 19 = 9
解包：d₂ = 1 ✓, d₃ = 2 ✓ (域 1 的两张照片完全未变)
```

只改了一个 cell，只改了一张照片的一个像素，其余 3 张照片完全不受影响。

---

## 第 4 章：项目版本历史与实验结果

### 4.1 核心可行性验证（v0.3 ~ v0.4）

**v0.3（2026-09-06）**：用 3 个固定素域存 6 张不同场景照片（人像、猫、咖啡、火箭、哈勃、硬币）写入同一个 base，独立进程中精确恢复所有照片。

| 结果 | 数值 |
|---|---|
| 最大通道误差 | ≤ 2（来自量化本身，不是 CRT 引入的） |
| PSNR | 46.33–46.39 dB（肉眼无法分辨） |
| Base 字节 vs 原始 RGB | 3.59 倍（**更大**，不是压缩） |

**v0.4（2026-09-06）**：增加到 12 张照片，动态生成素域，分块按需加载（RAM 峰值 34.55 MiB vs 全量加载 63.95 MiB）。

---

### 4.2 压缩实验（v0.5）

**问题**：base 的大整数字节被 zlib 几乎压不动（1.0× 比率），能改进吗？

**方案**：把每个 cell 的大整数拆成 2K 个 uint8 余数平面，对每个平面做行差分（Sub-delta）再用 lzma 压缩。

**理由**：每个平面恰好是一张量化照片的像素值，空间相关性强，lzma 能有效压缩。

| 结果 | 数值 |
|---|---|
| K=6（12 张照片）存储压缩比 | **2.79 倍** |
| PSNR（与原版相同） | 45.72–46.39 dB |
| 写入速度 | 比原版快 1.44 倍（I/O 减少） |

---

### 4.3 记忆遮挡实验（Direction 1）

**问题**：如果从 base 的余数平面里提取出每张照片对应的"平面"，对相关照片（同一张图但被遮挡）与无关照片，这两种平面的相关性有什么区别？

**实验设计**：  
- 原始照片 ↔ 裁剪版（保留中心 90%/80%/.../50%）  
- 原始照片 ↔ 随机掩码版（随机抹除 10%/.../50% 像素）

**关键发现**：

| 指标 | 裁剪 p=0.50 | 随机掩码 p=0.50 |
|---|---|---|
| 相关照片 r_related | 0.28 | 0.42 |
| 无关照片 r_unrelated | 0.07 | 0.04 |
| 差值（辨别力） | +0.21 | **+0.37** |

**直觉**：裁剪制造了"共享的零边框"，使无关照片基线被抬高；随机掩码没有共享结构，辨别力更纯粹。与分布式记忆理论（Kanerva 1988）一致：随机破坏比集中破坏对记忆信号的损害更小。

---

### 4.4 耦合容量实验（Direction 2）

**问题**：把 $s$ 张照片塞进同一个域（$s=1,2,3,4$），如何影响错误的传播方式？

**关键发现**：

1. **精确字段级联**（H_A1 PASS）：污染字段 0 → 恰好 $s$ 张照片降级，其余完全不变
2. **per-photo PSNR 与 $s$ 无关**（H_A4 PASS）：同等字段污染率下，每张照片质量相同，max 差值 0.23 dB
3. **同字段错误位置 100% 重叠**（H_A2 PASS）：同字段两张照片的错误位置 IoU = 0.977（理论 1.0）
4. **$s=1$ 是精确的"平凡独立"边界**：$s \geq 2$ 是非平凡耦合的充要条件

**实践含义**：选 $s=2$（当前系统）不会让每张照片质量更差，但使失效"捆绑"——你无法只让 2 张中的 1 张降级，它们永远一起失效。

---

## 第 5 章：代码结构与阅读指南

### 5.1 文件地图

```
SmartCoder/
├── field_factory.py          ← 域工厂：生成素数序列、CRT 权重（核心数学）
├── stream_memory.py          ← 分块存储引擎（v0.4 主实现）
├── run_stream_demo.py        ← 12 张照片的完整端到端演示
├── verify_base_case.py       ← 手算 base case 的自动验证脚本
│
├── per_residue_delta_zstd/   ← v0.5 压缩版本（不改动主实现）
│   └── stream_memory_cmp.py
│
├── field_crop_recall/        ← Direction 1：裁剪遮挡实验
│   └── run_crop_recall.py
│
├── field_corruption_v2/      ← Direction 1 v2：380 张照片 + 随机掩码
│   └── run_corruption_v2.py
│
└── coupling_capacity/        ← Direction 2：s-槽耦合实验
    └── run_coupling_exp.py
```

---

### 5.2 追踪一个像素的旅程

以下代码片段展示了一个像素从照片到 base 再回来的完整路径（简化版，保留核心逻辑）：

**Step 1：量化（照片 → 6-bit 整数）**

```python
import numpy as np
from PIL import Image

img = np.array(Image.open("photo.jpg").convert("RGB"))  # uint8 [0, 255]
quantized = img // 4                                      # uint8 [0, 63]  ← 6-bit
```

**Step 2：打包（两张照片 → 一个域残数）**

```python
Q = 64
# 假设 photo_0 和 photo_1 都已量化，shape = (H, W, 3)
residue_field_0 = photo_0 + Q * photo_1   # [0, Q²) = [0, 4096)
```

**Step 3：CRT 编码（多个域残数 → 一个大整数 cell）**

```python
# 见 field_factory.py 的 encode() 方法
b = sum(residue_k * weight_k for k, (residue_k, weight_k) in ...) % M
```

**Step 4：解码（大整数 cell → 域残数 → 像素）**

```python
# 读取域 0 的残数
r0 = b % prime_0

# 解包两张照片
d_photo_0 = r0 % Q          # 照片 0 的像素
d_photo_1 = r0 // Q         # 照片 1 的像素

# 反量化（近似还原到 8-bit）
pixel_0 = d_photo_0 * 4 + 2  # 误差 ≤ 2
```

---

### 5.3 field_factory.py 的关键代码

```python
# 文件: field_factory.py（原样复制，不要修改）

def family(q, count):
    """生成 count 个 ≥ q² 的最小素数，并计算 CRT 权重。"""
    primes = []
    n = q * q
    while len(primes) < count:
        if is_prime(n):
            primes.append(n)
        n += 1
    modulus = math.prod(primes)           # M = p₀ × p₁ × ... × p_{K-1}
    weights = tuple(
        (modulus // p * pow(modulus // p, -1, p)) % modulus   # e_k
        for p in primes
    )
    return tuple(primes), modulus, weights
```

`pow(a, -1, p)` 是 Python 3.8+ 内置的"模逆元"：等价于 $a^{-1} \bmod p$。

---

### 5.4 stream_memory.py 的写入核心

```python
# 文件: stream_memory.py，方法 write_roi（简化注释版）

def write_roi(self, photo, rgb, y=0, x=0):
    field, slot = divmod(photo, 2)          # 第几个域？第几个槽？
    p = self.factory.primes[field]          # 该域的素数
    e = self.factory.weights[field]         # 该域的 CRT 权重
    m = self.factory.modulus                # 总模数 M

    for each tile covering the ROI:
        values = load_tile(...)             # 加载该 tile 的 cell 大整数列表
        for each cell in ROI:
            residue = values[cell] % p      # 取出该域的当前残数
            old_digit = residue // (64**slot) % 64   # 当前像素值（d_old）
            new_digit = quantized_pixel                # 目标像素值（d_new）
            delta = (new_digit - old_digit) * (64**slot) % p
            values[cell] = (values[cell] + delta * e) % m   # ← 幂等元更新
        save_tile(...)                      # 写回
```

**关键：幂等元更新**保证了只修改当前域的信息，其他所有域的值不变。这就是为什么写入一张照片不会影响其他照片。

---

## 第 6 章：运行实验

### 6.1 环境配置

```bash
# 确认 Python 版本 ≥ 3.9
python3 --version

# 安装依赖
pip install numpy pillow
```

### 6.2 运行各实验

```bash
# 进入项目目录
cd /path/to/SmartCoder

# 【新人第一步】手算 base case 的自动验证（耗时 <1 秒）
python3 verify_base_case.py

# 【v0.4 主演示】12 张照片存储与恢复（需要 2-3 分钟）
python3 run_stream_demo.py

# 【Direction 1 遮挡实验】12 张照片的裁剪相关性（<10 秒）
python3 field_crop_recall/run_crop_recall.py

# 【Direction 1 v2】380 张照片 + 随机掩码（~3 分钟）
python3 field_corruption_v2/run_corruption_v2.py

# 【Direction 2 耦合实验】s=1,2,3,4 容量对比（~30 秒）
python3 coupling_capacity/run_coupling_exp.py
```

### 6.3 读懂输出

每个实验脚本都会在终端打印：
- 每个 Hypothesis（假设）的 PASS/FAIL
- 关键数字（PSNR、压缩比、相关系数等）
- 写入 `metrics.json` 保存完整数据

---

## 第 7 章：如何提出并实现自己的方向

### 7.1 AGENTS.md 规定的必要步骤（不可跳过）

在开始任何新实验之前，**必须按顺序完成**：

1. **文献调研**：找最新（2024-2026）相关论文，记录日期、链接、阅读范围
2. **数学对照**：列出你要用的数学对象定义、已有定理、与现有方案的区别
3. **手算 base case**：设计一个可以纸上算出来的小例子并验证
4. **预注册假设**：写出你的 Hypothesis（可证伪），定义指标和阈值，写下失败条件
5. **实现与报告**：实现代码，分别报告文献结论、理论推导、实测数值

### 7.2 一个新方向的文件模板

假设你想研究"什么时候两张照片应该放在同一个域（语义相似度驱动的域分配）"，你需要创建：

```
my_direction/
  RESEARCH_MY_DIRECTION.md   ← 文献+数学
  PLAN_MY_DIRECTION.md       ← 预注册假设（运行前写好，不能事后改）
  run_my_experiment.py       ← 实现
  RESULTS_MY_DIRECTION.md    ← 运行后写入（不得早于运行前写）
```

**PLAN 文件的最小格式**：

```markdown
# PLAN：[你的方向名]

## 实验设置
- 数据：用哪些照片
- 方法：具体做什么操作

## Hypothesis
| 编号 | 假设 | 指标 | 阈值 | 失败条件 |
|---|---|---|---|---|
| H1 | ... | ... | ... | ... |

阈值来源：[说明阈值从哪里来，不能是"看结果再定"]
```

---

## 第 8 章：练习题

### 练习 1（手算，10 分钟）

用 $Q=4$，$p_0=17$，$p_1=19$，$M=323$，CRT 权重 $e_0=171$，$e_1=153$。

存入以下 4 张照片在位置 0 的像素：
- 照片 A（域 0，槽 0）：像素 = 0
- 照片 B（域 0，槽 1）：像素 = 3
- 照片 C（域 1，槽 0）：像素 = 2
- 照片 D（域 1，槽 1）：像素 = 1

**求**：
(a) $r_0, r_1$（残数）  
(b) base cell 值 $b$  
(c) 验证 $b \bmod 17$ 和 $b \bmod 19$  
(d) 解码还原 4 张照片的像素值

---

### 练习 2（代码阅读，15 分钟）

打开 `field_factory.py`，回答：

(a) `family(q=64, count=3)` 会返回哪 3 个素数？（提示：找 ≥ 64² = 4096 的最小 3 个素数）  
(b) 为什么 `pow(modulus // p, -1, p)` 里的 `-1` 是个有效的 Python 参数？  
(c) 把 `family(q=4, count=2)` 的结果手工计算一遍，与 §3.4 的例子对比。

---

### 练习 3（代码修改，30 分钟）

在 `field_crop_recall/run_crop_recall.py` 里，函数 `center_crop` 目前做的是中心裁剪。

请修改，使它改为从左上角裁剪（保留左上角 $(1-p)^2$ 比例的像素）。

- 运行修改后的脚本，观察 r_related 曲线是否发生变化
- 在 PLAN_FIELD_CROP.md 中添加你的实验条件（需要手算预测结论再运行）

---

### 练习 4（提出方向，开放题）

结合第 4 章的三个已有方向，提出一个**你认为有趣的新实验**。要求：

- 用一句话说明你想测量什么
- 说明你的假设（如果能手算的话更好）
- 列出你需要修改哪些文件

---

## 附录 A：常用数学记号速查

| 记号 | 含义 | 例子 |
|---|---|---|
| $a \bmod m$ | a 除以 m 的余数 | $17 \bmod 5 = 2$ |
| $a^{-1} \bmod m$ | a 的模 m 逆元 | $2^{-1} \bmod 17 = 9$ |
| $a \equiv b \pmod{m}$ | a 和 b 模 m 同余 | $18 \equiv 1 \pmod{17}$ |
| $\lfloor x \rfloor$ | x 向下取整 | $\lfloor 11/4 \rfloor = 2$ |
| $\mathbb{Z}/m\mathbb{Z}$ | 模 m 的整数集合 | $\mathbb{Z}/17\mathbb{Z} = \{0,1,...,16\}$ |
| $F_p$ | 素数域（= $\mathbb{Z}/p\mathbb{Z}$） | $F_{17}$：17 个元素的有限域 |
| $M_K = \prod p_k$ | K 个素数的乘积 | $M_2 = p_0 \times p_1$ |
| $e_k$ | 域 k 的 CRT 权重（幂等元） | $e_k \bmod p_k = 1$，$e_k \bmod p_j = 0$（$j \neq k$） |

## 附录 B：练习题答案（练习 1）

**(a)** $r_0 = 0 + 4\times3 = 12$，$r_1 = 2 + 4\times1 = 6$

**(b)** $b = (12\times171 + 6\times153) \bmod 323 = (2052 + 918) \bmod 323 = 2970 \bmod 323$  
$2970 \div 323 = 9.18...$，$9 \times 323 = 2907$，$2970 - 2907 = 63$。所以 $b = 63$。

**(c)** $63 \bmod 17 = 63 - 3\times17 = 63-51 = 12$ ✓ （$= r_0$）  
$63 \bmod 19 = 63 - 3\times19 = 63-57 = 6$ ✓ （$= r_1$）

**(d)** 照片 A：$12 \bmod 4 = 0$ ✓；照片 B：$12 \div 4 = 3$ ✓；  
照片 C：$6 \bmod 4 = 2$ ✓；照片 D：$6 \div 4 = 1$ ✓
