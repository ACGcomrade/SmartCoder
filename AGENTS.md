# SmartCoder 研究与实现硬性规则

本规则适用于本目录下的所有后续功能、数学运算形式、编码/解码器、存储结构及更新策略的新增或实质修改。来源：用户 2026-09-06 的明确要求。

## 每项变更必须先通过研究门槛

在实现前，必须按以下顺序完成，并在研究记录中为该变更建立可追踪条目：

1. 检索相关技术栈的最新研究与原始文章，记录检索日、论文初稿/修订日期、链接及阅读范围。若未发现直接相关的新成果，记录实际检索范围，不用无关新论文代替。
2. 对照已有数学理论，列出对象、定义域/值域、定理条件、信息容量和与现有方案的区别。field 必须是数学域。
3. 给出可手算的 base case 与证明；保留反例及不适用条件。运行最小复算后才可扩展到实际数据。
4. 预先写 hypothesis、可证伪条件、指标、验收阈值和对照。数值阈值须有理论推导或明确标为实验约定，不得看过结果再迁就数据。
5. 实现并测试，分开报告文献结论、项目推导、实测结果和未证实的假说。更新 README、研究记录、实现计划和版本成果。

同一轮可将依赖相同理论的紧密相关变更合并调研，但每项都必须映射到来源、base case 与 hypothesis。既有来源可以复用；每次新增仍需做最新进展检索。

## 当前图像实验约束

- base 是二维状态；明确元素类型和 RGB 等额外索引如何表达。
- 数学域数量不得超过输入照片总数的一半；同时报告固定 decoder 数及所有会产生新映射的参数，禁止用隐藏的逐照片 decoder 绕过限制。
- 保存输入照片来源及哈希，保留原始尺寸和比例。预处理、量化和有损步骤必须披露。
- 恢复必须从完成全部写入的同一个最终 base 进行；恢复进程不读取原始照片或逐照片像素副本。
- 局部/整体读取与写入分别定义并实测；实际存储、metadata、decoder 参数和临时工作空间不能混为一谈。
- 未经实验证明，不将精确/有损编码与重建称为智能、自组织或压缩突破。

## v0.4 起的增长与 RAM 约束

- 使用一个容量参数化的域工厂和通用解码族；不得为新增照片手写专属数学解码方法。共用代码不免除实际 field/decoder 实例计数。
- 区分磁盘容量、活动块解压字节、Python 临时对象、进程峰值 RSS、OS 页缓存；文件映射或分块本身不是已测得的 RAM 优势。
- RAM 基准用独立进程、相同依赖、相同输出与紧凑全载基线，报告重复测量；高水位增量为零不能解释为零 RAM。
- 动态扩域必须证明旧记忆保持及新域初始化语义，报告迁移/追加读写成本；“容量驱动生成”不得误称“由内容学习到域”或智能涌现。
