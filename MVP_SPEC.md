# SmartCoder 最小实现规格与 base case 复算

**当前第三阶段 v0.4 已实现**：[PLAN_STREAMING.md](./PLAN_STREAMING.md) 固定12照片扩增、统一域生成、按需RAM及S1–S7；[RESEARCH_STREAMING.md](./RESEARCH_STREAMING.md) 是先行调研/推导；[RESULTS_STREAMING.md](./RESULTS_STREAMING.md) 是本机实测。运行 `python3 run_stream_demo.py`；程序创建独立run目录，保留旧版6图demo与其规格。单一逻辑二维base采用版本化空间块表示，不要求一次全部驻留RAM。

**第二阶段 v0.3 已实现**：二维照片记忆的最新规格和实现前 hypothesis 在 [PLAN_2D.md](./PLAN_2D.md)，系统构建与手算在 [RESEARCH_2D.md](./RESEARCH_2D.md)，验收在 [RESULTS_2D.md](./RESULTS_2D.md)。6 张照片、3 个固定数学域、每域恢复 2 张，使用同一个最终 base。运行 `python3 run_photo_demo.py`（在 SmartCoder 目录），依赖 NumPy/Pillow。以下为第一阶段规格与历史结果，不表示当前只有一维实现。

版本 v0.2.0，2026-09-06。依据 [RESEARCH.md](./RESEARCH.md) 的本轮调研和 [README.md](./README.md) 的 P1–P5 推导。构造 A 优先检验严格 one-to-one；构造 B 检验不同域的独立写入，并明确演示它不满足单 decoder 的强单射。

## 1. 第一阶段交付范围

只需 Python 标准库和一个有限状态验证脚本。数学对象使用精确整数与模运算实现；不使用浮点近似。第一阶段是读写与唯一性可行性证明，还未实现自组织学习。

文件：

- README.md：目标、假设、已有依据、完整推导、手算及版本日志。
- RESEARCH.md：本轮数学与前沿研究，包含初稿和修订日期、证据范围。
- MVP_SPEC.md：本规格及实际复算结果。
- verify_base_case.py：穷举正例、反例和局部读写追踪。

运行：

    python3 /Volumes/ACGcomrade_entelechy/kaiForever/SmartCoder/verify_base_case.py

## 2. 精确接口与状态

第一阶段冻结：
\[
M=6,\quad N=2,\quad A=\{0,\ldots,5\}.
\]
完整 base 为 \(B\in A^2\)，共 36 种状态。数学“有限 list”并不由程序语言的数据类型决定。

构造 A：

- decode_all(B, D)：输出所有坐标，完整读取 2 个 cell；带 D 类型后检验输入对单射。
- read(B, j, D)：只读取第 j 个 cell；返回局部数值，只对局部 base 声称可逆。
- write(B, j, D, y)：使用域中逆元恢复合法 b，再修改一个 cell；越界/不在 decoder 像集的 y 被拒绝。
- 两个 D 固定为 (7, 1, 1)、(11, 2, 1)，分别表示素数 p、乘数 α、偏移 β。

构造 B：

- project(B, j, p)：读取一 cell 并模 p。
- write_residue(B, j, p, v)：读取同一 cell，用 CRT 幂等元更新；其他 cell 和非目标域余数不变。
- pack(residues)：使用 CRT 公式重建 cell。
- 模数 2、3，载体是模 6 环。严禁将该环标为 \(F_6\)。

访问追踪必须在 get/set 接口实际发生时记录。原始事件、预期答案与全量验证快照只存在于测试侧；读写算法不得查询它们。完整检查所需的全量扫描单列为验证成本，不混入核心单步访问数。

## 3. 必须复算的例子

### A：严格对应、不同结果、局部改动

\[
B=(5,2),\quad D_7(B)=(6,3),\quad D_{11}(B)=(0,5).
\]
用 \(D_{11}\) 把第 1 个局部结果写为 7，逆元 6 给出
\[
6(7-1)=36\equiv3\pmod{11}.
\]
base 变成 \((3,2)\)，于是两个完整结果分别变成 \((4,3)\)、\((7,5)\)。单次写入 1/2 个 cell，之后局部回忆只读 1/2 个 cell。

更换 D 的 β 能在 base 不变时改变输出；例 \(d_{11}'(b)=2b+2\)，对 \(B=(5,2)\) 得 \((1,6)\)。两种参数设置都保持 α 非零。完整输入对需保存 decoder 版本/参数；只附域名称无法保证跨版本单射。

### A 的更强补充：无标签的严格单射

另外固定 \(h_7(b)=b\)、\(h_{13}(b)=b+6\)，仍只允许 \(0\le b<6\)。两者的规范数字像集分别为 0–5、6–11，互不相交。对 36 bases × 2 decoders 穷举，要求 72 个纯数值 list 全部不同；直接从结果区间识别 decoder 并逆解，不能读取附加标签。此例与上面的 D7/D11 家族分开，后者的无标签输出确实可能碰撞。

### B：不同域记忆的独立修改

\[
5\mapsto(1\in F_2,2\in F_3).
\]
把第一个余数改为 0，\(5+3\equiv2\pmod6\)，得到
\[
2\mapsto(0\in F_2,2\in F_3).
\]
这只改变一个域的记忆。随后对第二个 list 元素做任意写入，第一个元素中保留的记忆仍可读取。

反例必须同时展示：\(\pi_2(1)=\pi_2(3)\)，所以 B 模式不是 A 模式的严格单射替代。

## 4. 有限状态穷举与验收

这部分的命题是精确命题，验收为全部满足。人为选定 90% 或 99% 准确率不适用于这些代数不变量。

| 检验 | 穷举范围 | 必须成立的结果 |
|---|---|---|
| 域合法性 | \(F_2,F_3,F_7,F_{11}\) 的元素及运算 | 公理、非零逆元；模 6 的零因子反例 |
| A 输入对单射 | 36 bases × 2 decoders | 72 个带类型输出，72 个不同值；全部可逆 |
| A 无标签补充构造 | 36 bases × H7/H13 | 72 个纯数值输出不同；反解 base 与 decoder |
| A 局部写后读 | 36 × 2 decoders × 2 positions × 6 targets | 864 个案例成功；只写目标 cell |
| A 非法结果 | 两个 decoder 的完整 \(F_p\) 值域 | 像集外值拒绝，而非模 6 偷偷折回 |
| CRT cell 双射 | 6 个 cell 与 \(2\times3\) 余数组 | 完整往返正确 |
| CRT list 局部写入 | 36 bases × 2 positions × (2+3) targets | 360 个案例成功；非目标域和其他 cell 不变 |
| 记忆保持 | 6 个已写值 × 第二 cell 的 6 种更新 | 36 种历史；固定 decoder 从最终状态恢复先前值 |
| 动态 decoder | 36 bases 上切换 β | base 不变，输出按公式变化且可逆 |
| 负例 | 局部读、单余数、无类型结果 | 记录碰撞，禁止当作通过单射 |
| 可选擦除修复小例 | 下节的 2 个校验块 | 2 次读、1 次写恢复指定单擦除 |

A 的局部写入无需读取旧值；写后读单独计一次读取。CRT 更新需要读旧 cell 一次、写同一 cell 一次。即使写相同值仍计一次写操作，但实际 changed-cell 数为 0；二者分开。

## 5. 一个更强但单列的擦除恢复例子

依据 R5–R6 的恢复集合概念，采用自行展开的最简单单校验关系。扩展为两个块：
\[
C=(a,b,a+b,\ c,d,c+d)\in R^6,\quad R=\mathbb Z/6\mathbb Z.
\]
其每个域投影都是对应 \(F_2,F_3\) 上的线性单校验关系。例如
\[
C=(5,2,1,\ 1,4,5).
\]
若位置 1 的值被擦除且位置已知，读位置 2、3 得
\(a=1-2\equiv5\pmod6\)。只读取 2/6、写入 1/6；第三方 block 不访问。这个例子证明指定位置的一次已知擦除可恢复；不能声称一个任意未知错误可纠正。

正常将 a 改成 3 时，第三坐标也必须改为 5，故写 2/6。对于同一块发生两次擦除，这个校验不够：只知道 a+b=1 时有 6 组解。若一个被读取的邻居已经损坏，则上述恢复公式可能输出错误；不得把它描述为任意噪声鲁棒。

这个小例子增加了冗余，且属于 CRT 对照路线；不改变 A 的严格单射承诺，也没有自动证明“记忆涌现”。

## 6. 信息与成本核算

固定 decoder 的参数必须单列；它们在所有实例中复用且不能编码每条样本答案。若未来 decoder 在线更新，必须把其状态增长计入。

- 单 cell 的模 6 状态：6 种可能，信息量 \(\log_2 6\)；固定二进制编码 3 bits。
- 两 cell：36 种状态，整体最少 6 bits；逐 cell 固定编码也是 6 bits。
- A 的完整解码输出是计算产物；若另行持久化，就计入存储。
- B 的两个独立余数逐项定长存储需 1+2=3 bits；与打包无压缩差异。
- 两个单校验块共有 \(6^4\) 个合法状态；逐 cell 存储 18 bits，相对四个原始 cell 的 12 bits 增加冗余。合法状态的理论整体编码下界另为 \(\lceil\log_2 6^4\rceil=11\) bits；不能混用逐符号成本与整体编码下界。

访问 locality、算术操作数、实际 bit 变化量、serialized bytes 分开报告。对大 M，求模/乘法不是恒定 bit 成本。最小算例 M=6 的简短运算，只证明有限实例可行。

## 7. 后续最小实验及决策依据

完成本轮复算后，可依次增加：

1. 将 N 扩展、随机化写入历史；验证保持性质与状态边界，不训练新 decoder。
2. 选择需要的损坏模型：已知擦除、未知 bit 翻转、或残缺查询；分别设计冗余与评分。
3. 定义域上局部动力学和外部可计算目标，研究编码/修复算子是否能从经验中形成。有限域上不能直接沿用实数梯度而不作说明。
4. 检验未见规则组合与消融，包括 raw list、直接余数表示、固定算子、去除记忆状态。若结果来自手写算法，归为代数计算能力。
5. 根据结果选择严格全局单射的 A、独立多域记忆的 B，或提出新的构造。若需要两者全部优点，先面对 README P5 和非干扰限制，而非调参掩盖。

## 8. 本轮实测记录

2026-09-06 已运行上述命令，退出码 0；所有断言通过。无需安装依赖。

| 实测项 | 结果 |
|---|---|
| 素域运算检查 | 2、3、7、11 全部通过；确认模 6 存在非零零因子 |
| A 带类型输入对 | 72/72 个不同输出，并逐项逆解成功 |
| A 无标签补充构造 | 72/72 个不同的纯数值 list，全部能反解 base 与 decoder |
| A 局部写后读 | 864/864；其中 144 次重复写值实际改变 0 cell，720 次改变 1 cell |
| A 更新的 bit 变化 | 三 bit cell 中观察到 0、1、2、3 bit 变化，不能说“一 cell 就是一 bit” |
| 非法像集结果 | 6 个值全部被拒绝 |
| CRT cell 往返 | 6/6 |
| CRT 局部更新 | 360/360；非目标域及其他 cell 全部保持 |
| 写入后无关更新并序列化/重建 | 36/36 种历史保留原值；这是状态往返验证，不是跨进程恢复实验 |
| decoder 参数切换 | 36/36；不修改 base，结果变化且可逆 |
| 指定擦除恢复 | 读下标 2、1，写下标 0，恢复值 5（程序下标从 0 开始） |
| 带校验正常更新 | 读下标 1，写下标 0、2，得到 (3,2,5,1,4,5) |
| 两擦除反例 | a+b=1 有 6 组可能原值，无法唯一恢复 |

另外四个预期碰撞均已复现：单个 CRT 投影、局部查询对完整 base、D7/D11 家族的跨 decoder 无类型数值、只带域名而漏掉 decoder 参数的跨版本结果。这些是对应构造的性质不成立的证据；不否定 H7/H13 的无标签正例，也不被计作“成功记忆”。

本轮完成的是两个明确定义的数学构造和有限验证；没有运行大规模性能、压缩率、联想学习或智能评测。序列化 bytes 随编码格式变化，本轮未将 JSON 大小当作理论压缩率。
